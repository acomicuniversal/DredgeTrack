/*
 * Production configuration.
 * Copy this file's values from Supabase Dashboard â†’ Project Settings â†’ API.
 * The anon key is safe to expose in a browser; never put a service-role key here.
 */
window.DREDGETRACK_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  projectId: 'north-channel'
};

